# honeypot-stack

Este directorio queda reservado para extraer la infraestructura principal a un módulo reutilizable.

En esta primera versión, la definición vive directamente en `opentofu/main.tf` para mantener el arranque del proyecto simple y visible.
